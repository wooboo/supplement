#!/bin/bash

# 'hey' - A quick assistant for bash/Arch Linux tasks using OpenCode
# Uses Gemini 3 Flash for fast and cost-effective responses.

if [ -z "$1" ]; then
    echo "Usage: hey <prompt>"
    echo "Example: hey how to find all backup files in the current directory?"
    exit 1
fi

PROMPT="$*"

# Call opencode run with the model and prompt.
# Provider: google, Model: antigravity-gemini-3-flash
# We instruct it to be a Linux/Arch assistant and show exact commands/scripts.

opencode run -m google/antigravity-gemini-3-flash "You are a Linux/Arch Linux/Omarchy terminal assistant. Provide the exact command(s) or a short script to solve the following task. Be concise. If multiple steps are needed, list them clearly. Always show the full command or script. If you need clarifications, ask simple questions. Ask if the user wants to execute the command or copy it to the clipboard. Task: $PROMPT"
